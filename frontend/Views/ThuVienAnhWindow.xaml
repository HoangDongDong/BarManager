<Window x:Class="QuanLyBar.Client.Views.ThuVienAnhWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thư viện ảnh" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" Background="#f0f0f0">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" Padding="10" BorderBrush="LightGray" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Quản lý thư viện ảnh" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <!-- List of icons -->
            <Border BorderBrush="LightGray" BorderThickness="1" Background="White" Margin="0,0,10,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="IcDanhGia">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" Foreground="Blue" FontWeight="Bold"/>
                                        <Separator Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center" Background="#b0c4de"/>
                                    </Grid>
                                    <ListBox ItemsSource="{Binding Children}" 
                                             BorderThickness="0" Background="Transparent" 
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                             SelectionChanged="LbIcons_SelectionChanged"
                                             Margin="0,5,0,0">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel IsItemsHost="True"/>
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding ImageSource}" Width="32" Height="32" Margin="5" ToolTip="{Binding Name}"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Padding" Value="5"/>
                                                <Setter Property="Margin" Value="2"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="#e0f0ff"/>
                                                        <Setter Property="BorderBrush" Value="#0078d7"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                    </ListBox>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Action buttons -->
            <StackPanel Grid.Column="1">
                <Button x:Name="BtnThemMoi" Content="Thêm mới" Margin="0,0,0,10" Padding="5" Click="BtnThemMoi_Click"/>
                <Button x:Name="BtnThayThe" Content="Thay thế" Margin="0,0,0,10" Padding="5" IsEnabled="False" Click="BtnThayThe_Click"/>
                <Button x:Name="BtnXoa" Content="Xóa" Margin="0,0,0,30" Padding="5" IsEnabled="False" Click="BtnXoa_Click"/>
                
                <Button x:Name="BtnChon" Content="Chọn" Margin="0,0,0,10" Padding="5" FontWeight="Bold" IsEnabled="False" Click="BtnChon_Click"/>
                <Button x:Name="BtnThoat" Content="Thoát" Padding="5" Click="BtnThoat_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
