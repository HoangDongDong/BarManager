<Window x:Class="QuanLyBar.Client.Views.ChonPhongWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CHỌN PHÒNG" Height="500" Width="680"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded" KeyDown="Window_KeyDown">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Background="#f0f2f5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Filter Bar -->
        <Border Grid.Row="0" Background="#d2e3f3" BorderBrush="#b0c8e2" BorderThickness="0,0,0,1" Padding="10,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Lọc: " VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,5,0"/>
                <TextBox x:Name="TxtLoc" Width="200" Height="24" Padding="3,2" TextChanged="TxtLoc_TextChanged"/>
            </StackPanel>
        </Border>
        
        <!-- Grid list -->
        <DataGrid Grid.Row="1" x:Name="DgBan" AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectionMode="Single" HeadersVisibility="Column" 
                  GridLinesVisibility="All" AlternatingRowBackground="#f2f8fc" Background="White"
                  BorderThickness="0" RowHeight="24"
                  MouseDoubleClick="DgBan_MouseDoubleClick" PreviewKeyDown="DgBan_PreviewKeyDown">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#f4f4f4"/>
                    <Setter Property="BorderBrush" Value="#d4d4d4"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="6,4"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="40">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Path Data="M 0 0 L 6 4 L 0 8 Z" Fill="Black" VerticalAlignment="Center" Margin="2,0,0,0"
                                      Visibility="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}"/>
                                <TextBlock Grid.Column="1" Text="{Binding SttDisplay}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555555"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTemplateColumn Header="Chọn" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Phòng" Binding="{Binding Name}" Width="180" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Khu vực" Binding="{Binding KhuVucName}" Width="160"/>
                <DataGridTextColumn Header="Loại phòng" Binding="{Binding LoaiPhongName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Bottom Actions -->
        <Border Grid.Row="2" Background="#e4ecf4" BorderBrush="#c8d6e5" BorderThickness="0,1,0,0" Padding="10,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Width="90" Height="28" Margin="0,0,10,0" Background="#ffffff" BorderBrush="#a0b8d0" Click="BtnOK_Click" IsDefault="True"/>
                <Button Content="Hủy bỏ" Width="90" Height="28" Background="#ffffff" BorderBrush="#a0b8d0" Click="BtnHuyBo_Click" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
