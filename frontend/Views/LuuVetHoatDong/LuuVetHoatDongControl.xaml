<UserControl x:Class="QuanLyBar.Client.Views.LuuVetHoatDongControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">
             
    <Grid Background="#e5ecf3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>

        <!-- ======================= CỘT TRÁI: HÓA ĐƠN ======================= -->
        <Border Grid.Column="0" BorderBrush="#b6c1cd" BorderThickness="1" Background="White" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Tiêu đề và bộ lọc Hóa đơn -->
                <StackPanel Grid.Row="0">
                    <Border Background="#ffecb3" BorderBrush="#f7ca18" BorderThickness="0,0,0,1" Padding="5,2">
                        <TextBlock Text="HÓA ĐƠN" FontWeight="Bold" Foreground="#1e3f66"/>
                    </Border>
                    <Border Background="#e3eaf5" Padding="5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Từ:" VerticalAlignment="Center" Margin="0,0,5,5"/>
                            <DatePicker x:Name="dpTuNgay" Grid.Row="0" Grid.Column="1" Margin="0,0,10,5"/>
                            
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Đến:" VerticalAlignment="Center" Margin="0,0,5,5"/>
                            <DatePicker x:Name="dpDenNgay" Grid.Row="0" Grid.Column="3" Margin="0,0,5,5"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Số HĐ:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox x:Name="txtSoHd" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,10,0"/>
                            
                            <Button x:Name="btnTaiDuLieu" Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="2" Content="Tải dữ liệu" Padding="10,2" Click="BtnTaiDuLieu_Click"/>
                        </Grid>
                    </Border>
                </StackPanel>
                
                <!-- Lưới Hóa đơn -->
                <DataGrid x:Name="DgHoaDon" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="All" AlternatingRowBackground="#f9f9f9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Hóa đơn" Binding="{Binding SoPhieu}" Width="*"/>
                        <DataGridTextColumn Header="Bàn" Binding="{Binding Ban}" Width="100"/>
                        <DataGridTextColumn Header="Trạng thái" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- ======================= CỘT PHẢI: CHI TIẾT THAO TÁC ======================= -->
        <Border Grid.Column="1" BorderBrush="#b6c1cd" BorderThickness="1" Background="White" Margin="5,5,5,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Tiêu đề và bộ lọc Chi tiết -->
                <StackPanel Grid.Row="0">
                    <Border Background="#ffecb3" BorderBrush="#f7ca18" BorderThickness="0,0,0,1" Padding="5,2">
                        <TextBlock Text="CHI TIẾT THAO TÁC" FontWeight="Bold" Foreground="#1e3f66"/>
                    </Border>
                    <Border Background="#e3eaf5" Padding="5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Tài khoản:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox x:Name="cbTaiKhoan" Width="120" IsEditable="True" Margin="0,0,15,0"/>
                            
                            <TextBlock Text="Lọc dữ liệu:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox x:Name="txtLoc" Width="150"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                
                <!-- Lưới Chi tiết thao tác -->
                <DataGrid x:Name="DgLuuVet" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="All" AlternatingRowBackground="#f9f9f9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ngày" Binding="{Binding Ngay, StringFormat=dd/MM/yyyy}" Width="80"/>
                        <DataGridTextColumn Header="Giờ" Binding="{Binding Gio, StringFormat=HH:mm}" Width="60"/>
                        <DataGridTextColumn Header="Số đơn hàng" Binding="{Binding Sodonhang}" Width="100"/>
                        <DataGridTextColumn Header="Nội dung" Binding="{Binding Note}" Width="*"/>
                        <DataGridTextColumn Header="Tài khoản" Binding="{Binding Taikhoan}" Width="100"/>
                        <DataGridTextColumn Header="Thiết bị" Binding="{Binding Thietbi}" Width="100"/>
                        <DataGridTextColumn Header="Bàn" Binding="{Binding Ban}" Width="80"/>
                        <DataGridTextColumn Header="Chức năng" Binding="{Binding Chucnang}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
