<Window x:Class="QuanLyBar.Client.Views.ImportDinhLuongWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IMPORT ĐỊNH LƯỢNG" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#b9d1ea">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock TextWrapping="Wrap" Grid.Row="0" 
                   Text="Chức năng này cho phép bạn cập nhật nhanh định lượng từ excel mà không cần làm từng món một.&#x0a;&#x0a;Lưu ý: Sau khi cập nhật định lượng, các mặt hàng sẽ được tự động chuyển qua loại mặt hàng pha chế. Không sử dụng chức năng này cho mặt hàng combo."/>

        <StackPanel Grid.Row="1">
            <Button x:Name="BtnChonFileExcel" Content="Chọn file excel" Padding="5" Click="BtnChonFileExcel_Click" Background="White"/>
            <Button x:Name="BtnXuatFileMau" Content="Xuất file mẫu" Padding="5" Click="BtnXuatFileMau_Click" Background="White" Margin="0,5,0,0"/>
            <Button x:Name="BtnHuyBo" Content="Hủy bỏ" Padding="5" Click="BtnHuyBo_Click" Background="White" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</Window>
