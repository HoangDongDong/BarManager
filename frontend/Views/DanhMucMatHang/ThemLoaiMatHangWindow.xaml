<Window x:Class="QuanLyBar.Client.Views.ThemLoaiMatHangWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm loại mặt hàng" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="📄" FontSize="24" Margin="0,0,10,0"/>
            <TextBlock Text="Thêm loại mặt hàng" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>
        </StackPanel>

        <!-- Form -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Tên loại:" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtTenLoai" Grid.Column="1" Margin="5,0,0,0" Padding="4"/>
        </Grid>

        <!-- Note -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Ghi chú:" VerticalAlignment="Top" Margin="0,5,0,0"/>
            <TextBox x:Name="TxtGhiChu" Grid.Column="1" Margin="5,0,0,0" AcceptsReturn="True" TextWrapping="Wrap"/>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Ghi dữ liệu" Width="80" Margin="0,0,10,0" Click="BtnLuu_Click"/>
            <Button Content="Thoát" Width="80" Click="BtnThoat_Click"/>
        </StackPanel>
    </Grid>
</Window>
