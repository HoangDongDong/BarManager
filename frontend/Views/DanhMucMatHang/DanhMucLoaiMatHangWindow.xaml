<Window x:Class="QuanLyBar.Client.Views.DanhMucLoaiMatHangWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loại mặt hàng" Height="480" Width="550"
        WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header info -->
        <Border Grid.Row="0" Background="#fdfde4" BorderBrush="#e2dba0" BorderThickness="0,0,0,1" Padding="12,8">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🏷️" FontSize="26" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="Cho phép bạn quản lý và chỉnh sửa danh mục loại mặt hàng" VerticalAlignment="Center" FontWeight="Bold" FontSize="13"/>
            </StackPanel>
        </Border>

        <!-- ToolBar -->
        <ToolBar Grid.Row="1" Background="#dfe7f5" Padding="5">
            <Button x:Name="BtnThemMoi" Content="➕ Thêm mới" Margin="0,0,5,0" Padding="5,2" Click="BtnThemMoi_Click"/>
            <Button x:Name="BtnChinhSua" Content="✏️ Chỉnh sửa" Margin="0,0,5,0" Padding="5,2" Click="BtnChinhSua_Click"/>
            <Button x:Name="BtnXoa" Content="❌ Xóa" Margin="0,0,5,0" Padding="5,2" Click="BtnXoa_Click"/>
            <Separator/>
            <Button Content="🔄 Tải lại" Margin="0,0,5,0" Padding="5,2" Click="BtnTaiLai_Click"/>
        </ToolBar>

        <!-- Main List -->
        <DataGrid x:Name="DgLoaiMatHang" Grid.Row="2" Margin="8" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" MouseDoubleClick="DgLoaiMatHang_MouseDoubleClick"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="28">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Ảnh" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding IconSource}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Tên loại mặt hàng" Binding="{Binding Name}" Width="*"/>
                <DataGridCheckBoxColumn Header="Bán hàng" Binding="{Binding IsBanHang}" Width="75"/>
                <DataGridCheckBoxColumn Header="Tồn kho" Binding="{Binding IsTonKho}" Width="75"/>
                <DataGridCheckBoxColumn Header="Định lượng" Binding="{Binding IsDinhLuong}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#f0f0f0" BorderBrush="#d0d0d0" BorderThickness="0,1,0,0" Padding="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Thoát" Width="80" Height="28" Click="BtnThoat_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
