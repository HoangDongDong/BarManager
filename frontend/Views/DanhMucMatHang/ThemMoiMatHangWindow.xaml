<Window x:Class="QuanLyBar.Client.Views.ThemMoiMatHangWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MẶT HÀNG - THÊM MỚI" Height="600" Width="740"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">
    
    <Window.Resources>
        <ControlTemplate x:Key="ComboBoxWithFooterTemplate" TargetType="ComboBox">
            <Grid>
                <ToggleButton x:Name="ToggleButton" 
                              Focusable="false"
                              IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                              ClickMode="Press">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Border" 
                                    Background="White" 
                                    BorderBrush="#a4b8cc" 
                                    BorderThickness="1" 
                                    CornerRadius="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="1" Background="#f0f4f8" BorderBrush="#d0dbe8" BorderThickness="1,0,0,0">
                                        <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#556677" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                <ContentPresenter x:Name="ContentSite"
                                  IsHitTestVisible="False" 
                                  Content="{TemplateBinding SelectionBoxItem}"
                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                  Margin="5,3,23,3"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"/>
                <TextBox x:Name="PART_EditableTextBox"
                         Style="{x:Null}" 
                         Template="{x:Null}" 
                         HorizontalAlignment="Left" 
                         VerticalAlignment="Center" 
                         Margin="3,3,23,3"
                         Focusable="True" 
                         Background="Transparent"
                         Visibility="Hidden"
                         IsReadOnly="{TemplateBinding IsReadOnly}"/>
                <Popup x:Name="Popup"
                       Placement="Bottom"
                       IsOpen="{TemplateBinding IsDropDownOpen}"
                       AllowsTransparency="True" 
                       Focusable="False"
                       PopupAnimation="Slide">
                    <Border x:Name="DropDown"
                            MinWidth="{Binding ActualWidth, ElementName=ToggleButton}"
                            MaxHeight="260"
                            BorderThickness="1"
                            BorderBrush="#8baecc"
                            Background="White">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer x:Name="DropDownScrollViewer" MaxHeight="180" CanContentScroll="True">
                                <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained"/>
                            </ScrollViewer>
                            <Border Grid.Row="1" Background="#f4f7fc" BorderBrush="#c8d6e5" BorderThickness="0,1,0,0" Padding="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="1.3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Content="➕ Thêm" Margin="1" Padding="2,3" FontSize="11" Background="#eaf2fb" BorderBrush="#a4b8cc" Click="BtnThemLoai_Click"/>
                                    <Button Grid.Column="1" Content="🔄 Tải" Margin="1" Padding="2,3" FontSize="11" Background="#eaf2fb" BorderBrush="#a4b8cc" Click="BtnTaiLoai_Click"/>
                                    <Button Grid.Column="2" Content="Danh mục" Margin="1" Padding="2,3" FontSize="11" Background="#eaf2fb" BorderBrush="#a4b8cc" Click="BtnDanhMucLoai_Click"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </Popup>
            </Grid>
        </ControlTemplate>
        <Style x:Key="ComboBoxWithFooterStyle" TargetType="ComboBox">
            <Setter Property="Template" Value="{StaticResource ComboBoxWithFooterTemplate}"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ToolBar -->
        <ToolBar Grid.Row="0" Background="#dfe7f5">
            <Border BorderBrush="#a4b8cc" BorderThickness="1" CornerRadius="2" Background="#f4f8fc" Margin="0,0,2,0">
                <Menu Background="Transparent">
                    <MenuItem Padding="4,2" Cursor="Hand">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="13" Height="13" BorderBrush="#0055aa" BorderThickness="1.2" CornerRadius="1.5" Background="White" Margin="0,0,4,0" VerticalAlignment="Center">
                                    <Path Data="M 2,7 L 7,2 M 4,2 L 7,2 L 7,5" Stroke="#0055aa" StrokeThickness="1.2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Phím tắt" VerticalAlignment="Center" Foreground="#112233" FontSize="12"/>
                                <Path Data="M 0,0 L 3.5,4 L 7,0 Z" Fill="#556677" VerticalAlignment="Center" Margin="5,2,0,0"/>
                            </StackPanel>
                        </MenuItem.Header>
                        <MenuItem Header="Tạo mới" InputGestureText="(Ctrl+N)"/>
                        <MenuItem Header="Sao chép" InputGestureText="(Alt+C)"/>
                        <Separator/>
                        <MenuItem Header="Lưu" InputGestureText="(Ctrl+L)"/>
                        <MenuItem Header="Lưu &amp; Mới" InputGestureText="(Ctrl+S)"/>
                        <MenuItem Header="Lưu &amp; Thoát" InputGestureText="(Enter)"/>
                        <Separator/>
                        <MenuItem Header="Thoát" InputGestureText="(ESC)"/>
                    </MenuItem>
                </Menu>
            </Border>
            <Separator/>
            <Button x:Name="BtnTruoc" Padding="5,3" Click="BtnTruoc_Click" ToolTip="Mặt hàng trước (F10)">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="M 7,0 L 0,4.5 L 7,9 L 7,6 L 12,6 L 12,3 L 7,3 Z" Fill="#1b72cc" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="Trước (F10)" VerticalAlignment="Center" Foreground="#112233" FontSize="12"/>
                </StackPanel>
            </Button>
            <Button x:Name="BtnSau" Padding="5,3" Click="BtnSau_Click" Margin="2,0,0,0" ToolTip="Mặt hàng kế tiếp (F11)">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="M 5,0 L 12,4.5 L 5,9 L 5,6 L 0,6 L 0,3 L 5,3 Z" Fill="#1b72cc" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="Sau (F11)" VerticalAlignment="Center" Foreground="#112233" FontSize="12"/>
                </StackPanel>
            </Button>
            <Separator/>
            <Button x:Name="BtnTaoMoi" Padding="5,3" Click="BtnTaoMoi_Click" ToolTip="Tạo mới mặt hàng (Ctrl+N)">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Grid Width="14" Height="14" Margin="0,0,5,0" VerticalAlignment="Center">
                        <Path Data="M 1,1 L 8,1 L 11,4 L 11,13 L 1,13 Z" Fill="White" Stroke="#3a70a8" StrokeThickness="1"/>
                        <Ellipse Width="8" Height="8" Fill="#28a745" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-2,-2"/>
                        <Path Data="M 2,4 L 6,4 M 4,2 L 4,6" Stroke="White" StrokeThickness="1.2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-2,-2" Width="8" Height="8"/>
                    </Grid>
                    <TextBlock Text="Tạo mới" VerticalAlignment="Center" Foreground="#112233" FontSize="12"/>
                </StackPanel>
            </Button>
            <Button Content="Sao chép" Padding="5,3"/>
            <Separator/>
            <Button Content="❌ Xóa" Padding="5,3" HorizontalAlignment="Right"/>
        </ToolBar>

        <!-- Main Tab Control -->
        <TabControl x:Name="TabMain" Grid.Row="1" Margin="0,10,0,0" SelectionChanged="TabMain_SelectionChanged">
            <TabItem x:Name="TabThongTin" Header="Thông tin mặt hàng">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="195"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 0: Mã hàng -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã hàng" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="TxtMaHang" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Margin="5" Background="#E0FFFF"/>

                    <!-- Row 1: Tên hàng -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Tên hàng" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="TxtTenHang" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Margin="5" Background="#E0FFFF"/>

                    <!-- Row 2: Tên tiếng anh -->
                    <TextBlock x:Name="LblTenTiengAnh" Grid.Row="2" Grid.Column="0" Text="Tên tiếng anh" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="TxtTenTiengAnh" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="5"/>

                    <!-- Row 3: Giá bán -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Giá bán" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="TxtGiaBan" Grid.Row="3" Grid.Column="1" Margin="5" Text="0"/>
                    <TextBlock x:Name="LblGia2" Grid.Row="3" Grid.Column="2" Text="Giá 2" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox x:Name="TxtGia2" Grid.Row="3" Grid.Column="3" Margin="5" Text="0"/>

                    <!-- Row 4: Giá bán 3/4 -->
                    <TextBlock x:Name="LblGia3" Grid.Row="4" Grid.Column="0" Text="Giá 3" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox x:Name="TxtGia3" Grid.Row="4" Grid.Column="1" Margin="5" Text="0"/>
                    <TextBlock x:Name="LblGia4" Grid.Row="4" Grid.Column="2" Text="Giá 4" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox x:Name="TxtGia4" Grid.Row="4" Grid.Column="3" Margin="5" Text="0"/>

                    <!-- Row 5: ĐVT bán -->
                    <TextBlock x:Name="LblDvtBan" Grid.Row="5" Grid.Column="0" Text="ĐVT bán, định lượng" VerticalAlignment="Center" Margin="0,5"/>
                    <Grid Grid.Row="5" Grid.Column="1" Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="CboDvtBan" Background="#FFFFE0" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                        <Button Grid.Column="1" Content="➕" ToolTip="Thêm mới" Width="22" Margin="2,0,0,0" Click="BtnThemDvt_Click"/>
                        <Button Grid.Column="2" Content="🔄" ToolTip="Tải lại" Width="22" Margin="2,0,0,0" Click="BtnTaiDvt_Click"/>
                        <Button Grid.Column="3" Content="🗂️" ToolTip="Danh mục" Width="22" Margin="2,0,0,0" Click="BtnDanhMucDvt_Click"/>
                    </Grid>
                    <CheckBox x:Name="ChkGiaTheoThoiGia" Grid.Row="5" Grid.Column="2" Grid.ColumnSpan="2" Content="Giá theo thời giá" VerticalAlignment="Center" Margin="5"/>

                    <!-- Row 6: ĐVT nhập -->
                    <TextBlock x:Name="LblDvtNhap" Grid.Row="6" Grid.Column="0" Text="ĐVT nhập mua" VerticalAlignment="Center" Margin="0,5"/>
                    <Grid x:Name="GridDvtNhap" Grid.Row="6" Grid.Column="1" Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="CboDvtNhap" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                        <Button Grid.Column="1" Content="➕" ToolTip="Thêm mới" Width="22" Margin="2,0,0,0" Click="BtnThemDvt_Click"/>
                        <Button Grid.Column="2" Content="🔄" ToolTip="Tải lại" Width="22" Margin="2,0,0,0" Click="BtnTaiDvt_Click"/>
                        <Button Grid.Column="3" Content="🗂️" ToolTip="Danh mục" Width="22" Margin="2,0,0,0" Click="BtnDanhMucDvt_Click"/>
                    </Grid>
                    <TextBlock x:Name="LblQuyDoi" Grid.Row="6" Grid.Column="2" Text="Quy đổi" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox x:Name="TxtQuyDoi" Grid.Row="6" Grid.Column="3" Margin="5" Text="1"/>

                    <!-- Row 7: Giá nhập -->
                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Giá nhập" VerticalAlignment="Center" Margin="0,5"/>
                    <Grid Grid.Row="7" Grid.Column="1" Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtGiaNhap" Text="0"/>
                        <TextBlock x:Name="TxtDonViQuyDoiHienThi" Grid.Column="1" Text=" (/1)" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="#555"/>
                    </Grid>
                    <TextBlock Grid.Row="7" Grid.Column="2" Text="Giá vốn" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox x:Name="TxtGiaVon" Grid.Row="7" Grid.Column="3" Margin="5" Text="0"/>

                    <!-- Row 8: Nhóm mặt hàng -->
                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Nhóm mặt hàng" VerticalAlignment="Center" Margin="0,5"/>
                    <ComboBox x:Name="CboNhomMatHang" Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="3" Margin="5" Background="#E0FFE0" DisplayMemberPath="Name" SelectedValuePath="Id"/>

                    <!-- Row 9: Loại mặt hàng -->
                    <TextBlock Grid.Row="9" Grid.Column="0" Text="Loại mặt hàng" VerticalAlignment="Top" Margin="0,5"/>
                    <StackPanel Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="3" Margin="5">
                        <RadioButton x:Name="RdoPhaChe" GroupName="LoaiMatHangGroup" Content="Mặt hàng pha chế" Margin="0,2" IsChecked="True" Checked="RdoLoai_Checked"/>
                        <RadioButton x:Name="RdoKiemVatTu" GroupName="LoaiMatHangGroup" Content="Mặt hàng kiêm vật tư" Margin="0,2" Checked="RdoLoai_Checked"/>
                        <RadioButton x:Name="RdoVatTu" GroupName="LoaiMatHangGroup" Content="Vật tư nguyên liệu" Margin="0,2" Checked="RdoLoai_Checked"/>
                        <RadioButton x:Name="RdoMo" GroupName="LoaiMatHangGroup" Content="Mặt hàng mở" Margin="0,2" Checked="RdoLoai_Checked"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <RadioButton x:Name="RdoKhac" GroupName="LoaiMatHangGroup" Content="Khác" VerticalAlignment="Center" Checked="RdoLoai_Checked" Unchecked="RdoLoai_Checked"/>
                            <Grid x:Name="GridLoaiKhac" Margin="15,0,0,0" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ComboBox x:Name="CboLoaiMatHangKhac" Height="25" SelectedValuePath="Id"
                                          SelectionChanged="CboLoaiMatHangKhac_SelectionChanged"
                                          Style="{StaticResource ComboBoxWithFooterStyle}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="2,1">
                                                <Image Source="{Binding IconSource}" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="12"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <Button Grid.Column="1" Content="➕" ToolTip="Thêm loại mặt hàng" Width="24" Height="25" Margin="4,0,0,0" Click="BtnThemLoai_Click"/>
                                <Button Grid.Column="2" Content="🔄" ToolTip="Tải lại danh sách" Width="24" Height="25" Margin="2,0,0,0" Click="BtnTaiLoai_Click"/>
                                <Button Grid.Column="3" Content="🗂️" ToolTip="Danh mục loại mặt hàng" Width="24" Height="25" Margin="2,0,0,0" Click="BtnDanhMucLoai_Click"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>

                    <!-- Image Section -->
                    <Grid Grid.Row="0" Grid.RowSpan="7" Grid.Column="4" Margin="10,5,0,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Label "Ảnh:" -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Ảnh:" VerticalAlignment="Center" Margin="0,0,0,4" Foreground="#112233" FontSize="12"/>

                        <!-- 3 Action Buttons -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,6,0" VerticalAlignment="Top">
                            <!-- Button 1: Dán ảnh vừa chụp -->
                            <Button x:Name="BtnDanAnh" Width="26" Height="26" Margin="0,0,0,4" Padding="2" 
                                    ToolTip="Dán ảnh vừa chụp" Click="BtnDanAnh_Click" 
                                    Background="#fbfcfd" BorderBrush="#a4b8cc" Cursor="Hand">
                                <Canvas Width="16" Height="16">
                                    <!-- Back page -->
                                    <Path Data="M 4,1 L 11,1 L 14,4 L 14,11 L 4,11 Z" Fill="#edf2f7" Stroke="#6b8299" StrokeThickness="1"/>
                                    <!-- Front page -->
                                    <Path Data="M 1,4 L 8,4 L 11,7 L 11,15 L 1,15 Z" Fill="#ffffff" Stroke="#4a709c" StrokeThickness="1"/>
                                    <!-- Lines inside front page -->
                                    <Path Data="M 3,8 L 9,8 M 3,10 L 9,10 M 3,12 L 7,12" Stroke="#8fa5b8" StrokeThickness="1"/>
                                </Canvas>
                            </Button>

                            <!-- Button 2: Chọn ảnh từ thư viện -->
                            <Button x:Name="BtnChonFileAnh" Width="26" Height="26" Margin="0,0,0,4" Padding="2" 
                                    ToolTip="Chọn ảnh từ thư viện" Click="BtnChonFileAnh_Click" 
                                    Background="#fbfcfd" BorderBrush="#a4b8cc" Cursor="Hand">
                                <Canvas Width="16" Height="16">
                                    <!-- Folder back -->
                                    <Path Data="M 1,3 L 6,3 L 8,5 L 15,5 L 15,13 L 1,13 Z" Fill="#ffe082" Stroke="#d4a320" StrokeThickness="1"/>
                                    <!-- Folder front -->
                                    <Path Data="M 1,6 L 15,6 L 13,13 L 1,13 Z" Fill="#ffca28" Stroke="#d4a320" StrokeThickness="1"/>
                                    <!-- Green arrow -->
                                    <Path Data="M 9,7 L 14,2 M 10.5,2 L 14,2 L 14,5.5" Stroke="#2e7d32" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                </Canvas>
                            </Button>

                            <!-- Button 3: Xóa ảnh -->
                            <Button x:Name="BtnXoaAnh" Width="26" Height="26" Margin="0,0,0,0" Padding="2" 
                                    ToolTip="Xóa ảnh" Click="BtnXoaAnh_Click" 
                                    Background="#fbfcfd" BorderBrush="#a4b8cc" Cursor="Hand">
                                <Canvas Width="16" Height="16">
                                    <Path Data="M 3,3 L 13,13 M 13,3 L 3,13" Stroke="#d32f2f" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                </Canvas>
                            </Button>
                        </StackPanel>

                        <!-- Square Image Preview -->
                        <Border Grid.Row="1" Grid.Column="1" Width="150" Height="150" Background="White" BorderBrush="#707070" BorderThickness="1" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <Image x:Name="ImgMatHang" Stretch="Uniform" Margin="2"/>
                        </Border>
                    </Grid>
                    <StackPanel Grid.Row="7" Grid.RowSpan="3" Grid.Column="4" Margin="10,5,0,5">
                        <TextBlock>
                            <Hyperlink><Run Text="Lịch sử nhập mua hàng"/></Hyperlink>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,0">
                            <Hyperlink><Run Text="Lịch sử xuất bán hàng"/></Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem x:Name="TabDinhLuong" Header="Định lượng" PreviewMouseLeftButtonDown="TabDinhLuong_PreviewMouseLeftButtonDown">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ToolBar Grid.Row="0" Background="#f0f0f0">
                        <Button Content="➕ Thêm dòng (F4)" Padding="5" Click="BtnThemDong_Click"/>
                        <Button Content="❌ Xóa dòng (F8)" Padding="5" Click="BtnXoaDong_Click"/>
                        <Button Content="Xuất excel" Padding="5"/>
                        <Separator/>
                        <Label Content="Lọc" VerticalAlignment="Center"/>
                        <TextBox Width="150" Margin="5,0"/>
                    </ToolBar>
                    <DataGrid x:Name="DgDinhLuong" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" AlternatingRowBackground="#f9f9f9" GridLinesVisibility="All">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Vật tư" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SelectedMatHang.Name}" VerticalAlignment="Center" Margin="2,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.AllMaterials, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                  SelectedItem="{Binding SelectedMatHang, UpdateSourceTrigger=PropertyChanged}"
                                                  DisplayMemberPath="Name" 
                                                  IsEditable="True" 
                                                  IsTextSearchEnabled="True" 
                                                  StaysOpenOnEdit="True"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Số lượng" Width="80" Binding="{Binding SoLuong, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="ĐVT" Width="60" Binding="{Binding DonViTinh, Mode=OneWay}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Giá nhập" Width="90" Binding="{Binding GiaNhap, StringFormat=N0, Mode=OneWay}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Thành tiền nhập" Width="100" Binding="{Binding ThanhTienNhap, StringFormat=N0, Mode=OneWay}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Giá vốn" Width="90" Binding="{Binding GiaVon, StringFormat=N0, Mode=OneWay}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Thành tiền vốn" Width="100" Binding="{Binding ThanhTienVon, StringFormat=N0, Mode=OneWay}" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <Border Grid.Row="2" Background="#dfe7f5" Padding="5" BorderBrush="LightGray" BorderThickness="1,0,1,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="TỔNG" FontWeight="Bold" Margin="5,0"/>
                            <TextBlock Grid.Column="1" x:Name="TxtTongSoLuong" Text="0" FontWeight="Bold" HorizontalAlignment="Right" Margin="5,0"/>
                            <TextBlock Grid.Column="4" x:Name="TxtTongTienNhap" Text="0" FontWeight="Bold" HorizontalAlignment="Right" Margin="5,0"/>
                            <TextBlock Grid.Column="6" x:Name="TxtTongTienVon" Text="0" FontWeight="Bold" HorizontalAlignment="Right" Margin="5,0"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem Header="Khác">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <CheckBox x:Name="ChkTamKhoa" Grid.Row="0" Content="Tạm khóa" Margin="5"/>
                    <CheckBox Grid.Row="1" Content="Khối lượng theo cân" Margin="5"/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5,10,5,5">
                        <TextBlock Text="Tồn tối thiểu" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Width="80" Text="0"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="💾 Lưu" Width="80" Margin="0,0,10,0" Padding="5" Click="BtnLuu_Click"/>
            <Button Content="💾 Lưu &amp; Mới" Width="90" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Lưu &amp; Thoát" Width="90" Margin="0,0,10,0" Padding="5" Click="BtnLuuThoat_Click"/>
            <Button Content="Thoát" Width="80" Padding="5" Click="BtnThoat_Click"/>
        </StackPanel>
    </Grid>
</Window>
