<UserControl x:Class="QuanLyBar.Client.Views.ThongKeDoanhThuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200" Loaded="UserControl_Loaded">
             
    <Grid Background="#e5ecf3">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#e3eaf5" Padding="5" BorderBrush="#b6c1cd" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Từ:" VerticalAlignment="Center" Margin="5,0"/>
                <DatePicker x:Name="dpTuNgay" Width="100"/>
                <TextBlock Text="Đến:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <DatePicker x:Name="dpDenNgay" Width="100"/>
                
                <TextBlock Text="Cửa hàng:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <ComboBox Width="150" Margin="0,0,10,0"/>
                
                <Button Content="Refresh (F5)" Margin="10,0,5,0" Padding="10,2" Click="BtnRefresh_Click"/>
                <Button Content="In báo cáo" Margin="5,0,0,0" Padding="10,2"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Pane: Invoices and Summary -->
            <Grid Grid.Column="0" Margin="0,0,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <DataGrid x:Name="DgHoaDon" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="All" AlternatingRowBackground="#f9f9f9" SelectionChanged="DgHoaDon_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Số phiếu" Binding="{Binding SoPhieu}"/>
                        <DataGridTextColumn Header="Ngày" Binding="{Binding Ngay, StringFormat=dd/MM/yyyy}"/>
                        <DataGridTextColumn Header="Bàn" Binding="{Binding Ban}"/>
                        <DataGridTextColumn Header="Giờ vào" Binding="{Binding BatDau, StringFormat=HH:mm}"/>
                        <DataGridTextColumn Header="Giờ ra" Binding="{Binding KetThuc, StringFormat=HH:mm}"/>
                        <DataGridTextColumn Header="Giờ thanh toán" Binding="{Binding GioThanhToan, StringFormat=HH:mm}"/>
                        <DataGridTextColumn Header="Khách hàng" Binding="{Binding KhachHang}"/>
                        <DataGridTextColumn Header="Tiền giảm giá" Binding="{Binding TienGiamGia, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Tổng cộng" Binding="{Binding TongCong, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Thanh toán bởi"/>
                        <DataGridTextColumn Header="Tiền hàng" Binding="{Binding TienHang, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Ghi chú"/>
                        <DataGridTextColumn Header="Địa chỉ"/>
                        <DataGridTextColumn Header="Mã khách"/>
                        <DataGridTextColumn Header="Tỉ lệ giảm giá" Binding="{Binding TiLeGiamGia}"/>
                        <DataGridTextColumn Header="Diễn giải"/>
                        <DataGridTextColumn Header="Khách đưa" Binding="{Binding KhachDua, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Trả lại" Binding="{Binding TraLai, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Số phiếu đặt hàng"/>
                        <DataGridTextColumn Header="Nhân viên"/>
                        <DataGridTextColumn Header="Tiền mặt" Binding="{Binding TienMat, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Mã thẻ trả trước"/>
                        <DataGridTextColumn Header="Điện thoại"/>
                        <DataGridTextColumn Header="Tỉ lệ giảm giá giờ"/>
                        <DataGridTextColumn Header="Tiền giảm giá giờ" Binding="{Binding TienGiamGiaGio, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Số khách" Binding="{Binding SoKhach}"/>
                        <DataGridTextColumn Header="Đặt trước"/>
                        <DataGridTextColumn Header="Thẻ"/>
                        <DataGridTextColumn Header="Thẻ tt" Binding="{Binding TheThanhToan, StringFormat={}{0:N0}}"/>
                        <DataGridTextColumn Header="Giảm mặt hàng"/>
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- Summary Panel -->
                <Border Grid.Row="1" Background="#c8dcf0" BorderThickness="1" BorderBrush="#b6c1cd" Margin="0,5,0,0" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Cột 1 -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="TIỀN HÀNG:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTienHang" Grid.Row="0" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="TIỀN GIỜ:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTienGio" Grid.Row="1" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="GIẢM GIÁ TIỀN HÀNG:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtGiamGiaTienHang" Grid.Row="2" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="GIẢM GIÁ TIỀN GIỜ:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtGiamGiaTienGio" Grid.Row="3" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="TỔNG GIẢM GIÁ:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTongGiamGia" Grid.Row="4" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="PHÍ DỊCH VỤ:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtPhiDichVu" Grid.Row="5" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="THUẾ:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtThue" Grid.Row="6" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="7" Grid.Column="0" Text="TỔNG DOANH THU:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTongDoanhThu" Grid.Row="7" Grid.Column="1" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <!-- Dòng trống -->
                        <TextBlock Grid.Row="8" Grid.Column="0" Height="10"/>

                        <TextBlock Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2" Text="TỔNG THỰC THU (TIỀN MẶT + THẺ + CHUYỂN KHOẢN + THU - CHI):" FontWeight="Bold" Foreground="Green" Margin="0,5,0,0"/>
                        <TextBlock x:Name="TxtTongThucThu" Grid.Row="9" Grid.Column="2" Text="0" TextAlignment="Right" FontWeight="Bold" Foreground="Green" Margin="0,5,0,0"/>
                        
                        <!-- Cột 2 -->
                        <TextBlock Grid.Row="0" Grid.Column="3" Text="TIỀN MẶT:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTienMat" Grid.Row="0" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="3" Text="TIỀN THẺ:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTienThe" Grid.Row="1" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="3" Text="CHUYỂN KHOẢN:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtChuyenKhoan" Grid.Row="2" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="3" Grid.Column="3" Text="TRỪ TÍCH LŨY:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTruTichLuy" Grid.Row="3" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="4" Grid.Column="3" Text="THẺ TRẢ TRƯỚC:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtTheTraTruoc" Grid.Row="4" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="5" Grid.Column="3" Text="VOUCHER:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtVoucher" Grid.Row="5" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="6" Grid.Column="3" Text="CÔNG NỢ:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtCongNo" Grid.Row="6" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="7" Grid.Column="3" Text="THU KHÁC:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtThuKhac" Grid.Row="7" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                        
                        <TextBlock Grid.Row="8" Grid.Column="3" Text="CHI KHÁC:" FontWeight="Bold" Margin="0,2"/>
                        <TextBlock x:Name="TxtChiKhac" Grid.Row="8" Grid.Column="4" Text="0" TextAlignment="Right" FontWeight="Bold" Margin="0,2"/>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Right Pane: Invoice Details -->
            <Border Grid.Column="1" Background="White" BorderBrush="#b6c1cd" BorderThickness="1">
                <DataGrid x:Name="DgChiTiet" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="All" AlternatingRowBackground="#f9f9f9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã hàng" Width="80"/>
                        <DataGridTextColumn Header="Mặt hàng" Binding="{Binding TenMon}" Width="*"/>
                        <DataGridTextColumn Header="ĐVT" Binding="{Binding Dvt}" Width="50"/>
                        <DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGia, StringFormat={}{0:N0}}" Width="80"/>
                        <DataGridTextColumn Header="CK%" Binding="{Binding PhanTramGiamGia, StringFormat={}{0:N2}}" Width="40"/>
                        <DataGridTextColumn Header="Ghi chú" Width="100"/>
                        <DataGridTextColumn Header="S.lượng" Binding="{Binding SoLuong, StringFormat={}{0:N2}}" Width="60"/>
                        <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0}}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
