<Window x:Class="QuanLyBar.Client.Views.ThemMoiBanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BÀN - THÊM MỚI" Height="330" Width="450"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#f0f0f0" BorderBrush="#d4d4d4" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="🪑" FontSize="20" Margin="0,0,10,0"/>
                <TextBlock Text="Bàn" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Toolbar (mockup) -->
        <Border Grid.Row="1" Background="#eef3f9" BorderBrush="#d4d4d4" BorderThickness="0,0,0,1" Padding="5">
            <StackPanel Orientation="Horizontal">
                <Button Content="↗ Phím tắt ▾" Margin="0,0,5,0" Padding="5,2" Background="Transparent" BorderThickness="0" Foreground="SteelBlue"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,0"/>
                <Button x:Name="BtnTruoc" Content="⬅ Trước (F10)" Margin="5,0" Padding="5,2" Background="Transparent" BorderThickness="0" Foreground="SteelBlue" Click="BtnTruoc_Click"/>
                <Button x:Name="BtnSau" Content="➡ Sau (F11)" Margin="5,0" Padding="5,2" Background="Transparent" BorderThickness="0" Foreground="SteelBlue" Click="BtnSau_Click"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,0"/>
                <Button x:Name="BtnTaoMoi" Content="➕ Tạo mới" Margin="5,0" Padding="5,2" Background="Transparent" BorderThickness="0" Foreground="Green" Click="BtnTaoMoi_Click"/>
                <Button Content="📄 Sao chép" Margin="5,0" Padding="5,2" Background="Transparent" BorderThickness="0" Foreground="Gray"/>
                <Button Content="❌ Xóa" Margin="5,0" Padding="5,2" Background="Transparent" BorderThickness="0" Foreground="Gray"/>
            </StackPanel>
        </Border>

        <!-- Form Info inside TabControl -->
        <TabControl Grid.Row="2" Margin="10">
            <TabItem Header="Thông tin bàn">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Tên bàn" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                    <TextBox x:Name="TxtTenBan" Grid.Row="0" Grid.Column="1" VerticalContentAlignment="Center" Margin="0,2" Background="#e6ffff"/>

                    <TextBlock Text="Khu vực" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
                    <ComboBox x:Name="CmbKhuVuc" Grid.Row="1" Grid.Column="1" Margin="0,2" DisplayMemberPath="Name" SelectedValuePath="Id">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <ToggleButton x:Name="ToggleButton" 
                                                  Grid.Column="2" 
                                                  Focusable="false"
                                                  IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="20" />
                                                        </Grid.ColumnDefinitions>
                                                        <Border Grid.Column="1" Background="Transparent" BorderBrush="Transparent">
                                                            <Path x:Name="Arrow" Fill="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter x:Name="ContentSite"
                                                      IsHitTestVisible="False" 
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      Margin="3,3,23,3"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left" />
                                    <Popup x:Name="Popup"
                                           Placement="Bottom"
                                           IsOpen="{TemplateBinding IsDropDownOpen}"
                                           AllowsTransparency="True" 
                                           Focusable="False"
                                           PopupAnimation="Slide">
                                        <Grid x:Name="DropDown"
                                              SnapsToDevicePixels="True"                
                                              MinWidth="{TemplateBinding ActualWidth}"
                                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                            <Border x:Name="DropDownBorder"
                                                    Background="White"
                                                    BorderThickness="1"
                                                    BorderBrush="Gray"/>
                                            <ScrollViewer Margin="1,1,1,30" SnapsToDevicePixels="True">
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                            </ScrollViewer>
                                            <Border BorderThickness="0,1,0,0" BorderBrush="LightGray" VerticalAlignment="Bottom" Background="#f0f0f0">
                                                <UniformGrid Rows="1" Columns="3" Height="30">
                                                    <Button Content="➕ Thêm" Background="Transparent" BorderThickness="0" Foreground="Green"/>
                                                    <Button Content="🔁 Tải" Background="Transparent" BorderThickness="0" Foreground="Blue"/>
                                                    <Button Content="Danh mục" Background="Transparent" BorderThickness="0"/>
                                                </UniformGrid>
                                            </Border>
                                        </Grid>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>

                    <TextBlock Text="Nhóm hiển thị" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                    <ComboBox x:Name="CmbNhomHienThi" Grid.Row="2" Grid.Column="1" Margin="0,2" DisplayMemberPath="Name" SelectedValuePath="Id"/>

                    <TextBlock Text="Loại phòng" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0"/>
                    <ComboBox x:Name="CmbLoaiPhong" Grid.Row="3" Grid.Column="1" Margin="0,2" DisplayMemberPath="Name" SelectedValuePath="Id"/>

                    <TextBlock Text="Ghi chú" VerticalAlignment="Center" Grid.Row="4" Grid.Column="0"/>
                    <TextBox x:Name="TxtGhiChu" Grid.Row="4" Grid.Column="1" VerticalContentAlignment="Center" Margin="0,2"/>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer Buttons -->
        <Border Grid.Row="3" Background="#e6f2fa" BorderBrush="#d4d4d4" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0">
                <Button Content="💾 Lưu" Width="70" Margin="5" Background="White" Click="BtnLuu_Click"/>
                <Button Content="Lưu &amp; Mới" Width="80" Margin="5" Background="White"/>
                <Button Content="Lưu &amp; thoát" Width="90" Margin="5" Background="White"/>
                <Button Content="Thoát" Width="70" Margin="5" Background="White" Click="BtnThoat_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
