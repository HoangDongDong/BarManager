<Window x:Class="QuanLyBar.Client.Views.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đổi tên" Height="150" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#f0f0f0">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="LblPrompt" Text="Nhập tên mới:" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtInput" Grid.Row="1" Height="25" Padding="2" VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,15,0,0">
            <Button x:Name="BtnOK" Content="Đồng ý" Width="75" Margin="0,0,10,0" Click="BtnOK_Click" IsDefault="True"/>
            <Button x:Name="BtnCancel" Content="Hủy" Width="75" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
