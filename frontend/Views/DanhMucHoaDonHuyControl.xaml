<UserControl x:Class="QuanLyBar.Client.Views.DanhMucHoaDonHuyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Loaded="UserControl_Loaded">
    <Grid Background="#e5ecf3">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar lọc -->
        <Border Grid.Row="0" Background="#e3eaf5" Padding="5" BorderBrush="#b6c1cd" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Từ:" VerticalAlignment="Center" Margin="5,0"/>
                <DatePicker x:Name="dpTuNgay" Width="100"/>
                <TextBlock Text="Đến:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <DatePicker x:Name="dpDenNgay" Width="100"/>
                
                <TextBlock Text="Lọc:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <TextBox x:Name="txtLoc" Width="150" VerticalAlignment="Center" Margin="0,0,10,0"/>
                
                <Button Content="Refresh" Width="80" Padding="5,2" Click="BtnRefresh_Click"/>
            </StackPanel>
        </Border>
        
        <!-- Lưới Hóa đơn hủy -->
        <DataGrid x:Name="DgHoaDonHuy" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="All" AlternatingRowBackground="#f9f9f9" SelectionChanged="DgHoaDonHuy_SelectionChanged" Margin="5" ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ngày" Binding="{Binding Ngay, StringFormat=dd/MM/yyyy}" Width="80"/>
                <DataGridTextColumn Header="Số phiếu" Binding="{Binding SoPhieu}" Width="100"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="150"/>
                <DataGridTextColumn Header="Khách hàng" Binding="{Binding KhachHang}" Width="120"/>
                <DataGridTextColumn Header="Nhân viên" Binding="{Binding NhanVien}" Width="120"/>
                <DataGridTextColumn Header="Thu ngân hủy" Binding="{Binding ThuNganHuy}" Width="120"/>
                <DataGridTextColumn Header="Giờ hủy" Binding="{Binding GioHuy, StringFormat=HH:mm}" Width="70"/>
                <DataGridTextColumn Header="Ngày hủy" Binding="{Binding NgayHuy, StringFormat=dd/MM/yyyy}" Width="80"/>
                <DataGridTextColumn Header="Đổi trả" Binding="{Binding Doitra}" Width="70"/>
                <DataGridCheckBoxColumn Header="Đã thanh toán" Binding="{Binding DaThanhToan}" Width="90"/>
                <DataGridTextColumn Header="Giờ thanh toán" Binding="{Binding GioThanhToan, StringFormat=HH:mm}" Width="100"/>
                <DataGridTextColumn Header="Trả lại" Binding="{Binding TraLai, StringFormat={}{0:N0}}" Width="80"/>
                <DataGridTextColumn Header="Tiền hàng" Binding="{Binding TienHang, StringFormat={}{0:N0}}" Width="90"/>
                <DataGridTextColumn Header="Tỉ lệ thuế" Binding="{Binding TiLeThue, StringFormat={}{0:N2}}" Width="80"/>
                <DataGridTextColumn Header="Tiền thuế" Binding="{Binding TienThue, StringFormat={}{0:N0}}" Width="80"/>
                <DataGridTextColumn Header="Tỉ lệ giảm giá (%)" Binding="{Binding TiLeGiamGia, StringFormat={}{0:N2}}" Width="120"/>
                <DataGridTextColumn Header="Tiền giảm giá" Binding="{Binding TienGiamGia, StringFormat={}{0:N0}}" Width="100"/>
                <DataGridTextColumn Header="Thanh toán bởi" Binding="{Binding ThanhToanBoi}" Width="100"/>
                <DataGridTextColumn Header="Phí vận chuyển" Binding="{Binding PhiVanChuyen, StringFormat={}{0:N0}}" Width="100"/>
                <DataGridTextColumn Header="Lý do hủy" Binding="{Binding LyDoHuy}" Width="150"/>
                <DataGridTextColumn Header="Tiền giờ" Binding="{Binding TienGio, StringFormat={}{0:N0}}" Width="80"/>
                <DataGridTextColumn Header="Phí dịch vụ" Binding="{Binding PhiDichVu, StringFormat={}{0:N0}}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#b6c1cd"/>
        
        <!-- Lưới Chi tiết Hóa đơn hủy -->
        <DataGrid x:Name="DgChiTiet" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="All" AlternatingRowBackground="#f9f9f9" Margin="5" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã hàng" Binding="{Binding MaHang}" Width="80"/>
                <DataGridTextColumn Header="Tên hàng" Binding="{Binding TenHang}" Width="200"/>
                <DataGridTextColumn Header="ĐVT" Binding="{Binding Dvt}" Width="60"/>
                <DataGridTextColumn Header="Số lượng" Binding="{Binding SoLuong, StringFormat={}{0:N2}}" Width="70"/>
                <DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGia, StringFormat={}{0:N0}}" Width="90"/>
                <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0}}" Width="100"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
