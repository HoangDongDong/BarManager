<Window x:Class="QuanLyBar.Client.Views.NguoiDungPhanQuyen.ThemSuaNhomNguoiDungWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QuanLyBar.Client.Views.NguoiDungPhanQuyen"
        Title="Nhóm người dùng" Height="300" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#eef3f9" FontFamily="Segoe UI" FontSize="13">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="📝" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="TxtHeaderTitle" Text="Chỉnh sửa nhóm người dùng" FontSize="16" FontWeight="Bold" Foreground="#1e3f66" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Name & Icon -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Tên nhóm người dùng" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#2d3748"/>
            <TextBox Grid.Column="1" x:Name="TxtGroupName" Height="28" VerticalContentAlignment="Center" Padding="5,2" BorderBrush="#cbd5e1" Margin="0,0,10,0"/>

            <TextBlock Grid.Column="2" Text="Ảnh:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0" FontWeight="SemiBold" Foreground="#2d3748"/>
            <local:ImageGalleryPicker Grid.Column="3" x:Name="PickerAnh" Height="28" Width="65" HorizontalAlignment="Left"/>
        </Grid>

        <!-- Note -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Ghi chú:" VerticalAlignment="Top" Margin="0,5,0,0" FontWeight="SemiBold" Foreground="#2d3748"/>
            <TextBox Grid.Column="1" x:Name="TxtNote" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderBrush="#cbd5e1" Padding="5"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnSave" Content="Ghi dữ liệu" Width="100" Height="30" Margin="0,0,10,0"
                    Background="#3b82f6" Foreground="White" FontWeight="SemiBold" BorderThickness="0"
                    Cursor="Hand" Click="BtnSave_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button x:Name="BtnCancel" Content="Thoát" Width="80" Height="30"
                    Background="#e2e8f0" Foreground="#334155" FontWeight="SemiBold" BorderThickness="0"
                    Cursor="Hand" Click="BtnCancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
