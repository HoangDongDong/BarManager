# Quy tắc Git Push

- Chỉ thực hiện `git push` lên GitHub hoặc remote repository khi người dùng yêu cầu trực tiếp ("push", "push code", "push git",...).
- Khi người dùng yêu cầu "build", "sửa giao diện", hoặc thực hiện các tác vụ khác, không tự ý thực hiện `git push`.
